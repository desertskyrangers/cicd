<settings
		xmlns="http://maven.apache.org/SETTINGS/1.0.0"
		xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0 http://maven.apache.org/xsd/settings-1.0.0.xsd"
>

	<servers>
		<server>
			<id>acm</id>
			<username>${env.ACM_MAVEN_REPO_USERNAME}</username>
			<password>${env.ACM_MAVEN_REPO_PASSWORD}</password>
		</server>
		<server>
			<id>acm-release</id>
			<username>${env.ACM_MAVEN_REPO_USERNAME}</username>
			<password>${env.ACM_MAVEN_REPO_PASSWORD}</password>
		</server>
		<server>
			<id>acm-snapshot</id>
			<username>${env.ACM_MAVEN_REPO_USERNAME}</username>
			<password>${env.ACM_MAVEN_REPO_PASSWORD}</password>
		</server>
		<server>
			<id>acm-plugin</id>
			<username>${env.ACM_MAVEN_REPO_USERNAME}</username>
			<password>${env.ACM_MAVEN_REPO_PASSWORD}</password>
		</server>
		<server>
			<id>central-mirror</id>
			<username>${env.ACM_MAVEN_REPO_USERNAME}</username>
			<password>${env.ACM_MAVEN_REPO_PASSWORD}</password>
		</server>
	</servers>

	<mirrors>
		<mirror>
			<id>central-mirror</id>
			<name>Mirror to Acromere Maven Repository</name>
			<url>https://repo.acromere.com/central</url>
			<mirrorOf>central</mirrorOf>
		</mirror>
	</mirrors>

	<profiles>
		<profile>
			<id>signjar</id>
			<properties>
				<jarsigner.keystore>.github/keystore</jarsigner.keystore>
				<jarsigner.keystore.password>${env.ACM_KEYSTORE_PASSWORD}</jarsigner.keystore.password>
			</properties>
		</profile>
	</profiles>

</settings>
